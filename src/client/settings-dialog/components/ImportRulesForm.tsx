import { Button } from '@mui/material';
import React from 'react';

export const ImportRulesForm = () => {
  return (
    <Button
      variant="contained"
      onClick={() => {
        alert('Hello hello!');
      }}
    >
      Well hello there!
    </Button>
  );
};
